# Caddyfile for Transcription Pipeline
# NOTE: This is documentation only. The actual Caddyfile used by Caddy
# is at /home/peptifit/peptifit/caddy/Caddyfile
# Auth is handled by FastAPI API key middleware + React login screen.

transcribe.delboysden.uk {
    encode zstd gzip
    reverse_proxy transcription-pipeline:8000
}
